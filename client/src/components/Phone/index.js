import React from "react";

// Depending on the current path, this component sets the "active" class on the appropriate navigation link item
const Phone = (props) => {
    return (
        <div>{props.phonevalue}</div>
    );
}

export default Phone;
